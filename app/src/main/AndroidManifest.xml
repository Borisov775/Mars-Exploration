<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.marsexploration">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application

        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/newicon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/newicon"
        android:supportsRtl="true"
        android:theme="@style/Theme.MARSExploration"
        android:exported="true">
        <activity android:name=".ActivityLander" />
        <activity android:name=".ActivitySat" />
        <activity android:name=".LanderActivity"/>
        <activity android:name=".SatelliteActivity" />
        <activity android:name=".NoteAditor" />
        <activity android:name=".Notes" />
        <activity android:name=".Activity2" />
        <activity android:name=".PdfActivity" />
        <activity android:name=".MainActivity"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service android:name=".GeoLoggingService" />
    </application>

</manifest>